services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - jhu-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    networks:
      - jhu-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jhu-frontend.rule=Host(`jobhuntingu.com`) || Host(`www.jobhuntingu.com`)"
      - "traefik.http.routers.jhu-frontend.entrypoints=websecure"
      - "traefik.http.routers.jhu-frontend.tls=true"
      - "traefik.http.routers.jhu-frontend.tls.certresolver=http"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - db
    networks:
      - jhu-network
    expose:
      - "3001"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jhu-backend.rule=(Host(`jobhuntingu.com`) || Host(`www.jobhuntingu.com`)) && PathPrefix(`/api`)"
      - "traefik.http.routers.jhu-backend.entrypoints=websecure"
      - "traefik.http.routers.jhu-backend.tls=true"
      - "traefik.http.routers.jhu-backend.tls.certresolver=http"
      - "traefik.http.services.jhu-backend.loadbalancer.server.port=3001"

volumes:
  db_data:

networks:
  jhu-network:
    driver: bridge
